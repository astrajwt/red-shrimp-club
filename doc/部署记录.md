# 红虾俱乐部 — 腾讯云部署记录

> 部署日期：2026-03-15 | 域名绑定：2026-03-16

## 服务器信息

| 项目 | 值 |
|------|------|
| 云厂商 | 腾讯云 轻量应用服务器 |
| IP | 82.157.4.63 |
| 配置 | 2核4G / 59G SSD / 5Mbps |
| 系统 | Ubuntu 22.04 LTS (5.15.0-164-generic) |
| SSH 用户 | ubuntu |
| 项目路径 | /home/ubuntu/redshrimp/ |

## 已安装组件

| 组件 | 版本 | 用途 |
|------|------|------|
| Node.js | 22.22.1 | 后端运行时 |
| npm | 10.9.4 | 包管理 |
| PostgreSQL | 14 | 数据库 |
| Nginx | 1.18.0 | 反向代理 + 静态文件 |
| cloudflared | 2026.3.0 | Cloudflare Tunnel（免费 HTTPS） |

## 数据库

```
Host: 127.0.0.1:5432
DB:   redshrimp
User: redshrimp
Pass: redshrimp123
```

已执行的迁移：
- `backend-src/src/db/schema.sql` — 主表结构（19张表）
- `backend-src/src/db/push-migration.sql` — push_subscriptions 表

## Systemd 服务

### redshrimp（后端）

```ini
# /etc/systemd/system/redshrimp.service
[Unit]
Description=Red Shrimp Lab Backend
After=network.target postgresql.service

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/redshrimp/backend-src
ExecStart=/usr/bin/npx tsx src/index.ts
Restart=on-failure
RestartSec=5
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
```

常用命令：
```bash
sudo systemctl status redshrimp
sudo systemctl restart redshrimp
sudo journalctl -u redshrimp -f          # 实时日志
```

### cloudflared-tunnel（HTTPS 隧道）

```ini
# /etc/systemd/system/cloudflared-tunnel.service
[Unit]
Description=Cloudflare Tunnel (Quick)
After=network.target nginx.service

[Service]
Type=simple
User=ubuntu
ExecStart=/usr/bin/cloudflared tunnel --url http://127.0.0.1:80
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

查看当前 Tunnel URL：
```bash
sudo journalctl -u cloudflared-tunnel | grep trycloudflare
```

> **注意**：Quick Tunnel 域名每次服务重启会变。备案域名通过后应切换为固定域名。

## 域名

| 项目 | 值 |
|------|------|
| 域名 | shrimp.red |
| 注册商 | 帝思普（DNSPod） |
| 注册时间 | 2026-03-16 |
| 到期时间 | 2027-03-16 |
| DNS 解析 | A 记录 `@` + `www` → `82.157.4.63` |
| HTTPS | Let's Encrypt（certbot） |

### SSL 证书申请

DNS 生效后执行（一次性操作）：

```bash
sudo certbot --nginx -d shrimp.red -d www.shrimp.red \
  --non-interactive --agree-tos -m YOUR_EMAIL
```

验证 DNS 是否生效：
```bash
dig +short shrimp.red  # 应返回 82.157.4.63
```

certbot 成功后会自动修改 Nginx 配置，添加 443 监听和证书路径。证书每 90 天自动续期（certbot timer 已内置）。

---

## Nginx 配置

配置文件：`/etc/nginx/sites-available/redshrimp`

```nginx
server {
    listen 80 default_server;
    server_name shrimp.red www.shrimp.red;

    root /home/ubuntu/redshrimp/frontend-src/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /internal/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
    }

    location /socket.io/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /uploads/ {
        proxy_pass http://127.0.0.1:3001;
    }

    location /health {
        proxy_pass http://127.0.0.1:3001;
    }
}
```

## 后端 .env

```bash
# /home/ubuntu/redshrimp/backend-src/.env
PORT=3001
HOST=127.0.0.1
NODE_ENV=production
CORS_ORIGIN=*
LOCAL_AUTH_BYPASS=true

JWT_SECRET=<auto-generated>

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=redshrimp
DB_USER=redshrimp
DB_PASSWORD=redshrimp123

UPLOADS_DIR=/home/ubuntu/redshrimp/uploads
OBSIDIAN_ROOT=/home/ubuntu/redshrimp/vault

VAPID_PUBLIC_KEY=BJWixx1fsUETGpUIeYDRsL9KS9o-IIGAFxMHIOCnOBf2J-Dtn1bWN6uxp-XlyqXk3ItrvaDqlDdp3eoK3ENaU_g
VAPID_PRIVATE_KEY=<secret>
VAPID_SUBJECT=mailto:admin@redshrimp.local
```

## PWA 推送通知

### 架构
```
iPhone Safari → 添加到主屏幕 → PWA
    ↓ (授权通知)
Service Worker ← Web Push ← 后端 web-push 库
    ↓
iOS 系统通知
```

### VAPID 密钥
- 生成方式：`npx web-push generate-vapid-keys`
- 公钥同时配置在后端 `.env` 和前端 `.env.local`
- 公钥通过 `GET /api/push/vapid-key` 也可动态获取

### 推送触发场景
| 事件 | 通知内容 |
|------|---------|
| 新消息 | 发送者名称 + 消息预览 |
| 任务状态变更 | 任务标题 + 新状态 |
| Agent 崩溃 | Agent 名称 + 异常提示 |

### 相关文件
- 前端：`frontend-src/src/lib/push.ts`、`frontend-src/public/sw.js`
- 后端：`backend-src/src/services/push-notifications.ts`、`backend-src/src/routes/push.ts`
- DB：`push_subscriptions` 表

## 目录结构（服务器）

```
/home/ubuntu/redshrimp/
├── backend-src/          # 后端源码
│   ├── .env              # 环境变量
│   ├── node_modules/
│   └── src/
├── frontend-src/         # 前端源码
│   ├── dist/             # 构建产物（Nginx 直接 serve）
│   └── node_modules/
├── uploads/              # 文件上传目录
└── vault/                # Obsidian vault（agent 文档）
```

## 更新部署流程

从本地推送代码更新：

```bash
# 1. 上传代码
cd ~/workspace/slock-clone
rsync -az --exclude='node_modules' --exclude='.git' --exclude='.env' \
  backend-src/ ubuntu@82.157.4.63:/home/ubuntu/redshrimp/backend-src/
rsync -az --exclude='node_modules' --exclude='.git' \
  frontend-src/ ubuntu@82.157.4.63:/home/ubuntu/redshrimp/frontend-src/

# 2. 服务器上安装依赖 + 构建
ssh ubuntu@82.157.4.63 "cd /home/ubuntu/redshrimp/backend-src && npm install"
ssh ubuntu@82.157.4.63 "cd /home/ubuntu/redshrimp/frontend-src && npm install && npm run build"

# 3. 重启后端
ssh ubuntu@82.157.4.63 "sudo systemctl restart redshrimp"
```

## 踩过的坑

1. **Nginx 403 Permission Denied** — Nginx 以 `www-data` 用户运行，无权访问 `/home/ubuntu/`。解决：`chmod 755 /home/ubuntu`
2. **Quick Tunnel 域名不固定** — 每次 cloudflared 重启域名会变，iPhone 需要重新添加到主屏幕。绑定固定域名后已解决。

## 待办

- [x] 注册域名 shrimp.red（2026-03-16）
- [x] DNSPod 添加 A 记录 `@` + `www` → `82.157.4.63`
- [x] Nginx `server_name` 改为 `shrimp.red www.shrimp.red`
- [ ] **运行 certbot 申请 SSL 证书**（等 DNS 生效后执行）
- [ ] 腾讯云安全组确认 443 端口已开放
- [ ] 停用 cloudflared-tunnel（HTTPS 已由 Nginx + certbot 接管）
