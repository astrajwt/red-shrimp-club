# 开发日志 — 2026-03-12

> Agent: Alice (Developer)

---

## Phase 1 后端开发进度

### 已完成文件

| 文件 | 说明 |
|------|------|
| `backend-src/package.json` | 依赖配置（Fastify + Socket.io + pg + Bull） |
| `backend-src/src/db/schema.sql` | 完整 PostgreSQL Schema（19 张表） |
| `backend-src/src/db/client.ts` | pg Pool 封装 + 类型化查询助手 |
| `backend-src/src/daemon/logger.ts` | 三路日志（DB + Obsidian文件 + WebSocket） |
| `backend-src/src/daemon/events.ts` | 事件总线（所有 Daemon 事件的统一出口） |
| `backend-src/src/daemon/process-manager.ts` | Agent 进程管理（spawn/stop/restart/心跳） |
| `backend-src/src/index.ts` | Fastify 主入口 + Socket.io 集成 |
| `backend-src/src/routes/auth.ts` | Auth API（login/register/logout/refresh/me） |
| `backend-src/src/routes/messages.ts` | 消息 API（历史分页 + 发送 + sync） |
| `backend-src/src/routes/tasks.ts` | 任务 API（CRUD + claim + doc关联 + skill关联） |
| `backend-src/src/socket/index.ts` | Socket.io 实时通信（token验证 + 房间 + 心跳） |
| `backend-src/.env.example` | 环境变量模板 |
| `backend-src/setup.sh` | 阿里云裸机部署脚本（Node+PG+systemd） |

### 待完成

- [x] `routes/channels.ts` — 频道 CRUD + DM + 未读计数 ✅
- [x] `routes/agents.ts` — Agent 创建/启动/停止/活动状态 ✅
- [x] `routes/files.ts` — 文件上传（图片/PDF）✅
- [x] `routes/daemon.ts` — Daemon 健康检查 + Cron + Obsidian 接口 ✅（@Astra 扩展）
- [x] `src/daemon/scheduler.ts` — Cron 执行器 + 心跳调度 + Token 耗尽转派 ✅
- [x] `src/daemon/llm-client.ts` — 多模型 Provider（Claude/Kimi/OpenAI）+ 429 退避重试 ✅
- [x] API 限流：429 指数退避（3s→6s→…→max 60s，内置在 llm-client.ts）✅
- [x] 前端接入后端 ✅（见 Phase 2）
- [ ] Android 客户端（SlockApp）指向新后端

### 新增文件

| 文件 | 说明 |
|------|------|
| `backend-src/src/routes/channels.ts` | 频道 CRUD + DM + 未读计数 |
| `backend-src/src/routes/agents.ts` | Agent 管理 + 启停 + 心跳 + 日志分页 |
| `backend-src/src/routes/files.ts` | 多部分文件上传（图片≤10MB，PDF≤50MB）|
| `backend-src/src/routes/daemon.ts` | Daemon 内部路由 + Cron + Obsidian Git 同步 |
| `backend-src/src/daemon/scheduler.ts` | Cron 执行 + 心跳监控 + Token 转派调度 |
| `backend-src/src/daemon/llm-client.ts` | Claude/Kimi/OpenAI 统一接口 + 退避重试 |

### 架构决策记录

- **后端**：Node.js + Fastify + TypeScript（ESM）
- **数据库**：PostgreSQL（无 Redis，单机够用）
- **实时通信**：Socket.io（保持与现有 SlockApp 兼容）
- **部署**：裸机 + systemd（不用 Docker）
- **日志**：三路输出（DB + Obsidian + WebSocket实时推）
- **认证**：JWT（15min access + 30天 refresh，token hash存DB）

---

## Phase 2 前端接入进度

### 前端新建文件（`frontend-src/`）

| 文件 | 说明 |
|------|------|
| `package.json` | React 19 + Zustand + Socket.io-client + Tailwind |
| `vite.config.ts` | 开发代理 → localhost:3001，WebSocket 代理 |
| `src/lib/api.ts` | 类型化 API 客户端（auto refresh + 401 redirect）|
| `src/lib/socket.ts` | Socket.io 客户端（13 类型化事件）|
| `src/store/auth.ts` | Zustand auth store |
| `src/pages/LoginPage.tsx` | 接真实 JWT 认证 |
| `src/pages/ChannelsView.tsx` | 频道 + 实时消息 + 未读计数 |
| `src/pages/AgentsPage.tsx` | 启停 Agent + 实时状态 |
| `src/pages/TasksBoard.tsx` | 任务看板 + 文档点击标已读 |
| `src/pages/ActivityPage.tsx` | 实时日志流 + Agent 树 |
| `src/pages/DocumentViewer.tsx` | Obsidian markdown 渲染（真实 API）|

### 后端修复

- `index.ts` — 注册 `app.authenticate` decorator（各路由 preHandler 需要）
- `index.ts` — 移除无效 `db` import

---

*由 @Alice 实时记录*

---

## Phase 2 补丁 — 前端类型修复 + SettingsPage

### 修复

| 文件 | 修复内容 |
|------|---------|
| `src/lib/api.ts` | `Agent.status` 添加 `'online'` 状态（AgentsPage 引用此状态，TypeScript 报错）|
| `src/lib/api.ts` | `Agent` 接口添加 `model_provider: string \| null` 字段（后端已返回，前端类型缺失）|

### 新增

| 文件 | 说明 |
|------|------|
| `src/pages/SettingsPage.tsx` | 设置页：API Keys（Anthropic/Moonshot/OpenAI）+ Obsidian Root + Cron Jobs CRUD |
| `src/App.tsx` | 导航栏添加 `settings` 入口（顶部右侧，独立于主 NAV 列表）|

### SettingsPage 功能

- **API Keys 区**：显示各 provider 是否已配置（masked），允许填写新 key 保存到后端 `.env`
- **Obsidian Root**：配置 vault 路径供 Docs 页面使用
- **Cron Jobs 区**：完整 CRUD — 列表展示 / 新建（选 shrimp + cron 表达式 + prompt）/ 启停切换 / 删除
- **Danger Zone**：使用 Zustand auth store 的 `logout()` 方法安全退出

### 构建验证

```
✓ 339 modules transformed
✓ built in 1.31s (0 TypeScript errors)
```
