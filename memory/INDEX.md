# Memory Index

这是 `slock-clone` 在当前仓库位置下的知识入口。

## 使用方式

1. 在后端配置 `OBSIDIAN_ROOT=/home/jwt/JwtVault/slock-clone`
2. 打开当前 Doc 页面
3. 在左侧目录进入 `memory/`

## 约定

- 项目文档放在 `memory/`
- 技能文档放在 `skill/`
- 新增 Skill 也统一建在 `skill/<skill-name>/SKILL.md`
