# Memory

这个目录用于存放当前 `slock-clone` 项目的 Obsidian 文档内容。

约定：

- 当前 Doc 页面建议将 `OBSIDIAN_ROOT` 指向仓库根目录：`/home/jwt/JwtVault/slock-clone`
- `memory/` 作为项目知识库目录，所有长期文档、方案、记录优先放这里
- 内容跟随当前 GitHub 仓库一起同步，不再依赖旧位置

建议结构：

- `memory/architecture/` 架构设计
- `memory/product/` 产品文档
- `memory/dev-log/` 开发记录
- `memory/runbook/` 运维手册
